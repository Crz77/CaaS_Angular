import { NewShopFormErrorMessages } from './new-shop-form-error-messages';

describe('NewShopFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new NewShopFormErrorMessages()).toBeTruthy();
  });
});
