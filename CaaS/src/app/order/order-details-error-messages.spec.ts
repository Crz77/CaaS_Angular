import { OrderDetailsErrorMessages } from './order-details-error-messages';

describe('OrderDetailsErrorMessages', () => {
  it('should create an instance', () => {
    expect(new OrderDetailsErrorMessages()).toBeTruthy();
  });
});
